let jwt;_00a‍.x([["default",()=>_00a‍.o]]);_00a‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
  const token = ctx.cookies.get('access_token');
  if (!token) return next(); //토큰 X
  try {
    const decoede = jwt.verify(token, process.env.JWT_SECRET);
    _00a‍.g.console.log('decoede = ' + decoede);
    return next();
  } catch (e) {
    //검증 실패
    return next();
  }
};

_00a‍.d(jwtMiddleware);
